[package]
name = "sudoku-solver"
version = "0.1.0"
edition = "2024"

[features]
default = ["basic"]
basic = []
grid_bit_masking = []
grid_bit_masking_bitset = []

[dev-dependencies]
criterion = "0.7.0"

[dependencies]

[[bench]]
name = "benchmarks"
harness = false